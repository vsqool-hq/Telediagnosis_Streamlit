# URL backendu (Fly.io). Lokalnie: http://localhost:8080
NEXT_PUBLIC_API_BASE=http://localhost:8080

# Opcjonalny token (musi być taki sam jak TELEDIAG_API_TOKEN w backendzie).
# Zostaw pusty, jeśli backend nie wymaga tokenu.
NEXT_PUBLIC_API_TOKEN=
