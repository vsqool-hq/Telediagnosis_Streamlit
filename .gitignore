# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Dane runtime (lokalny katalog danych backendu)
backend/data/
data/

# Dane startowe (seed) — realne pliki biznesowe klienta (cennik, słownik kontrahentów).
# Nie wersjonujemy; potrzebne lokalnie/na serwerze tylko przy ZUPEŁNIE świeżej instalacji
# (patrz TELEDIAG_SEED_DIR w backend/start-local.command) — istniejące wdrożenia mają
# dane już zaimportowane i nigdy do tych plików nie wracają (app/seed.py: seed_if_empty).
seed_data/

# Node / Next.js
node_modules/
.next/
out/
.vercel/

# Środowisko
.env
.env.local
frontend/.env
frontend/.env.local
sync.env

# System
.DS_Store
*.log

# Statyczne dane aplikacji (geokody jednostek) — wersjonowane mimo reguły data/
!backend/app/data/
!backend/app/data/unit_geo.json

# Artefakt inkrementalnego typechecku TypeScript — nie wersjonujemy
frontend/tsconfig.tsbuildinfo
